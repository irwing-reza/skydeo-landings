{
  "name": "skydeo-landings",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run check && npm run build && wrangler deploy",
    "shape-seo-hook": "node .agent-hooks/shape-seo-on-new-page.mjs",
    "cf-typegen": "wrangler types",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^5.20260719.1",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.112.0"
  }
}
